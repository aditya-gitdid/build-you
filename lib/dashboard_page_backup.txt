import 'dart:async';import 'dart:math' as math;import 'package:cloud_firestore/cloud_firestore.dart';import 'package:firebase_auth/firebase_auth.dart';import 'package:flutter_map/flutter_map.dart';import 'package:geolocator/geolocator.dart';import 'package:flutter/material.dart';import 'package:image_picker/image_picker.dart';import 'package:latlong2/latlong.dart';import 'package:sensors_plus/sensors_plus.dart';import 'activity_calendar_page.dart';import 'activity_session.dart';import 'activity_status_page.dart';import 'chat_page.dart';import 'events_page.dart';import 'food_nutrition_page.dart';import 'profile_view_page.dart';enum TrackActivityType { running, walking, cycling }class DashboardPage extends StatefulWidget {  const DashboardPage({super.key});  @override  State<DashboardPage> createState() => _DashboardPageState();}class _DashboardPageState extends State<DashboardPage> {  int _currentTab = 0;  Timer? _timer;  Duration _elapsed = Duration.zero;  bool _isTracking = false;  bool _isPaused = false;  bool _gpsAcquired = false;  String _gpsStatusText = 'GPS not acquired';  double _distanceKm = 0;  double _currentSpeedKmh = 0;  int _steps = 0;  double _calories = 0;  Position? _lastPosition;  LatLng? _currentLatLng;  final List<LatLng> _routePoints = [];  final MapController _mapController = MapController();  StreamSubscription<Position>? _positionSubscription;  StreamSubscription<AccelerometerEvent>? _accelSubscription;  // Step detection  double _lastMagnitude = 0;  bool _stepPending = false;  static const double _stepThreshold = 11.5;  static const double _stepLow = 9.5;  StreamSubscription<QuerySnapshot<Map<String, dynamic>>>? _activitySubscription;  final FirebaseFirestore _firestore = FirebaseFirestore.instance;  List<ActivitySession> _activitySessions = [];  TrackActivityType _selectedActivity = TrackActivityType.running;  String _profileName = '';  final _friendSearchController = TextEditingController();  final _postCaptionController = TextEditingController();  final _postLocationController = TextEditingController();  final ImagePicker _imagePicker = ImagePicker();  String? _selectedImagePath;  bool _showPastEvents = false;  final _eventTitleController = TextEditingController();  final _eventDescriptionController = TextEditingController();  final _eventLocationController = TextEditingController();  DateTime? _eventDate;  TimeOfDay? _eventTime;  // â”€â”€ Metrics used in Profile tab â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€  double _sevenDayKm = 0;  int _sevenDayWorkouts = 0;  double _sevenDayCalories = 0;  int _dayStreak = 0;  final List<_EventItem> _upcomingEvents = [    _EventItem(      title: 'Sunday Morning Run',      type: 'Running',      dateLabel: 'Sunday, Feb 23',      timeLabel: '7:00 AM',      locationLabel: 'Central Park Â· 5K',      organizer: 'Sarah Miller',      joinedCount: 24,      capacity: 30,      joined: true,      accent: const Color(0xFFFF6200),      participantsPreview: ['ðŸ§‘â€ðŸ¦±', 'ðŸ‘©', 'ðŸ‘§', 'ðŸ‘¨â€ðŸ¦³'],      participantOverflow: 20,    ),    _EventItem(      title: 'Weekend Trail Walk',      type: 'Walking',      dateLabel: 'Saturday, Feb 22',      timeLabel: '9:00 AM',      locationLabel: 'Forest Trail Â· 3.5K',      organizer: 'Mike Chen',      joinedCount: 15,      capacity: 20,      joined: false,      accent: const Color(0xFFFF8B2D),      participantsPreview: ['ðŸ‘©', 'ðŸ§‘', 'ðŸ‘§', 'ðŸ§”'],      participantOverflow: 11,    ),    _EventItem(      title: 'City Cycling Group',      type: '\ud83d\udeb4 Cycling',      dateLabel: 'Friday, Feb 28',      timeLabel: '6:30 PM',      locationLabel: 'City Loop Â· 12K',      organizer: 'Emma Wilson',      joinedCount: 12,      capacity: 18,      joined: true,      accent: const Color(0xFFE53935),      participantsPreview: ['ðŸ‘¨', 'ðŸ‘©â€ðŸ¦±', 'ðŸ§‘â€ðŸ¦±', 'ðŸ‘¦'],      participantOverflow: 8,    ),  ];  final List<_PastEventItem> _pastEvents = [    _PastEventItem(title: '5K Charity Run', dateLabel: 'Feb 15, 2026', distance: '5K', participants: 45),    _PastEventItem(title: 'Beach Walk', dateLabel: 'Feb 10, 2026', distance: '4K', participants: 18),  ];  final List<String> _friendSuggestions = [    'Alex Cooper',    'Nina Patel',    'Ravi Kumar',    'Olivia Smith',    'Liam Johnson',  ];  final List<_SocialPost> _posts = [    _SocialPost(      userName: 'Sarah Miller',      timeAgo: '2 hours ago',      caption: 'Completed a 10K run! ðŸƒ',      imageUrl:          'https://images.unsplash.com/photo-1476480862126-209bfaa8edc8?auto=format&fit=crop&w=1200&q=80',      likes: 124,      comments: 18,      distance: '10.2 km',      duration: '52 min',      calories: '642',    ),    _SocialPost(      userName: 'Mike Chen',      timeAgo: '5 hours ago',      caption: 'Beautiful morning walk in the park ðŸŒ³',      imageUrl:          'https://images.unsplash.com/photo-1448375240586-882707db888b?auto=format&fit=crop&w=1200&q=80',      likes: 19,      comments: 12,      distance: '3.5 km',      duration: '45 min',      calories: '180',    ),    _SocialPost(      userName: 'Emma Wilson',      timeAgo: 'Yesterday',      caption: 'Pool session complete! Feeling refreshed ðŸ’ª',      imageUrl:          'https://images.unsplash.com/photo-1541534741688-6078c6bfb5c5?auto=format&fit=crop&w=1200&q=80',      likes: 67,      comments: 9,      distance: '2.0 km',      duration: '40 min',      calories: '320',    ),  ];  @override  void initState() {    super.initState();    _subscribeToActivities();    _fetchProfileName();  }  // â”€â”€ Firestore activity subscription â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€  void _subscribeToActivities() {    final uid = FirebaseAuth.instance.currentUser?.uid;    if (uid == null) return;    _activitySubscription = _firestore        .collection('users')        .doc(uid)        .collection('activities')        .snapshots()        .listen((snapshot) {      if (!mounted) return;      final sessions = snapshot.docs          .map((d) => ActivitySession.fromFirestore(d.id, d.data()))          .toList();      setState(() {        _activitySessions = sessions;        _recalculateMetrics();      });    });  }  void _recalculateMetrics() {    final now = DateTime.now();    final sevenDaysAgo = now.subtract(const Duration(days: 7));    final recent = _activitySessions        .where((s) => s.completedAt.isAfter(sevenDaysAgo))        .toList();    _sevenDayKm = recent.fold(0.0, (sum, s) => sum + s.distanceKm);    _sevenDayWorkouts = recent.length;    _sevenDayCalories = recent.fold(0.0, (sum, s) => sum + s.calories);    // Simple streak: count consecutive days with at least one session    int streak = 0;    for (int i = 0; i < 30; i++) {      final day = DateTime(now.year, now.month, now.day - i);      final hasSession = _activitySessions.any((s) =>          s.completedAt.year == day.year &&          s.completedAt.month == day.month &&          s.completedAt.day == day.day);      if (hasSession) {        streak++;      } else {        break;      }    }    _dayStreak = streak;  }  Future<void> _fetchProfileName() async {    final uid = FirebaseAuth.instance.currentUser?.uid;    if (uid == null) return;    final doc = await FirebaseFirestore.instance.collection('users').doc(uid).get();    if (!mounted) return;    setState(() {      _profileName = (doc.data()?['name'] as String? ?? '').trim();    });  }  @override  void dispose() {    _timer?.cancel();    _positionSubscription?.cancel();    _accelSubscription?.cancel();    _activitySubscription?.cancel();    _mapController.dispose();    _friendSearchController.dispose();    _postCaptionController.dispose();    _postLocationController.dispose();    _eventTitleController.dispose();    _eventDescriptionController.dispose();    _eventLocationController.dispose();    super.dispose();  }  double _activityMet() {    switch (_selectedActivity) {      case TrackActivityType.running:        return 9.8;      case TrackActivityType.walking:        return 3.8;      case TrackActivityType.cycling:        return 7.5;    }  }  Future<bool> _ensureLocationReady() async {    final serviceEnabled = await Geolocator.isLocationServiceEnabled();    if (!serviceEnabled) {      if (!mounted) return false;      ScaffoldMessenger.of(context).showSnackBar(        const SnackBar(content: Text('Please enable GPS/Location services.')),      );      return false;    }    var permission = await Geolocator.checkPermission();    if (permission == LocationPermission.denied) {      permission = await Geolocator.requestPermission();    }    if (permission == LocationPermission.denied ||        permission == LocationPermission.deniedForever) {      if (!mounted) return false;      ScaffoldMessenger.of(context).showSnackBar(        const SnackBar(content: Text('Location permission is required to track activity.')),      );      return false;    }    return true;  }  Future<void> _startTracking() async {    if (_isTracking) return;    final ready = await _ensureLocationReady();    if (!ready) return;    Position initial;    try {      initial = await Geolocator.getCurrentPosition(        locationSettings: const LocationSettings(accuracy: LocationAccuracy.bestForNavigation),      );    } catch (_) {      if (!mounted) return;      ScaffoldMessenger.of(context).showSnackBar(        const SnackBar(content: Text('Unable to fetch location. Try moving to open area.')),      );      return;    }    final startLatLng = LatLng(initial.latitude, initial.longitude);    setState(() {      _elapsed = Duration.zero;      _distanceKm = 0;      _currentSpeedKmh = 0;      _steps = 0;      _calories = 0;      _lastPosition = initial;      _currentLatLng = startLatLng;      _routePoints        ..clear()        ..add(startLatLng);      _isTracking = true;      _isPaused = false;      _gpsAcquired = true;      _gpsStatusText = 'GPS Acquired';    });    try {      _mapController.move(startLatLng, 17);    } catch (_) {}    // GPS stream    _positionSubscription?.cancel();    _positionSubscription = Geolocator.getPositionStream(      locationSettings: const LocationSettings(        accuracy: LocationAccuracy.bestForNavigation,        distanceFilter: 3,      ),    ).listen((position) {      if (!mounted) return;      final newLatLng = LatLng(position.latitude, position.longitude);      if (_lastPosition != null) {        final traveled = Geolocator.distanceBetween(          _lastPosition!.latitude, _lastPosition!.longitude,          position.latitude, position.longitude,        );        _distanceKm += traveled / 1000;      }      _lastPosition = position;      _currentSpeedKmh = math.max(0, position.speed * 3.6);      _calories = _computeCalories(_elapsed, _distanceKm);      setState(() {        _currentLatLng = newLatLng;        _routePoints.add(newLatLng);        _gpsAcquired = true;        _gpsStatusText = 'GPS Acquired';      });      try {        _mapController.move(newLatLng, 17);      } catch (_) {}    });    // Accelerometer step detection    _accelSubscription?.cancel();    _accelSubscription = accelerometerEventStream().listen((event) {      final mag = math.sqrt(event.x * event.x + event.y * event.y + event.z * event.z);      if (_lastMagnitude < _stepThreshold && mag >= _stepThreshold && !_stepPending) {        _stepPending = true;      } else if (_stepPending && mag <= _stepLow) {        _stepPending = false;        if (_isTracking) setState(() => _steps++);      }      _lastMagnitude = mag;    });    // Timer    _timer = Timer.periodic(const Duration(seconds: 1), (_) {      if (!mounted) return;      setState(() {        _elapsed += const Duration(seconds: 1);        _calories = _computeCalories(_elapsed, _distanceKm);      });    });  }  double _computeCalories(Duration elapsed, double distanceKm) {    final hours = elapsed.inSeconds / 3600;    return _activityMet() * 70 * hours;  }  void _stopTracking() {    _timer?.cancel();    _timer = null;    _positionSubscription?.cancel();    _positionSubscription = null;    _accelSubscription?.cancel();    _accelSubscription = null;    setState(() {      _isTracking = false;      _isPaused = false;      _gpsStatusText = _gpsAcquired ? 'GPS Acquired' : 'GPS not acquired';    });  }  void _pauseTracking() {    _timer?.cancel();    _timer = null;    _positionSubscription?.cancel();    _positionSubscription = null;    _accelSubscription?.cancel();    _accelSubscription = null;    setState(() {      _isPaused = true;      _gpsStatusText = 'Paused';    });  }  Future<void> _resumeTracking() async {    final ready = await _ensureLocationReady();    if (!ready) return;    _positionSubscription = Geolocator.getPositionStream(      locationSettings: const LocationSettings(        accuracy: LocationAccuracy.bestForNavigation,        distanceFilter: 3,      ),    ).listen((position) {      if (!mounted) return;      final newLatLng = LatLng(position.latitude, position.longitude);      if (_lastPosition != null) {        final t = Geolocator.distanceBetween(          _lastPosition!.latitude, _lastPosition!.longitude,          position.latitude, position.longitude,        );        _distanceKm += t / 1000;      }      _lastPosition = position;      _currentSpeedKmh = math.max(0, position.speed * 3.6);      setState(() {        _currentLatLng = newLatLng;        _routePoints.add(newLatLng);        _gpsAcquired = true;        _gpsStatusText = 'GPS Acquired';      });      try {        _mapController.move(newLatLng, 17);      } catch (_) {}    });    _accelSubscription = accelerometerEventStream().listen((event) {      final mag = math.sqrt(event.x * event.x + event.y * event.y + event.z * event.z);      if (_lastMagnitude < _stepThreshold && mag >= _stepThreshold && !_stepPending) {        _stepPending = true;      } else if (_stepPending && mag <= _stepLow) {        _stepPending = false;        if (_isTracking && !_isPaused) setState(() => _steps++);      }      _lastMagnitude = mag;    });    _timer = Timer.periodic(const Duration(seconds: 1), (_) {      if (!mounted) return;      setState(() {        _elapsed += const Duration(seconds: 1);        _calories = _computeCalories(_elapsed, _distanceKm);      });    });    setState(() {      _isPaused = false;      _gpsStatusText = 'GPS Acquired';    });  }  Future<void> _toggleTracking() async {    if (_isTracking) {      final sessionDistance = _distanceKm;      final sessionSpeed = _currentSpeedKmh;      final sessionTime = _elapsed;      final sessionSteps = _steps;      final sessionCalories = _calories;      final sessionType = _selectedActivity;      _stopTracking();      if (sessionTime.inSeconds > 0) {        await _saveCompletedSession(          distance: sessionDistance,          duration: sessionTime,          speed: sessionSpeed,          steps: sessionSteps,          calories: sessionCalories,          type: sessionType,        );        await _showCompletionDialog();        if (!mounted) return;        Navigator.push(          context,          MaterialPageRoute(            builder: (_) => ActivityStatusPage(              title: sessionType == TrackActivityType.running                  ? 'Evening Run'                  : sessionType == TrackActivityType.walking                      ? 'Evening Walk'                      : 'Evening Cycling',              distanceKm: sessionDistance,              movingTime: _formatDuration(sessionTime),              avgSpeedKmh: sessionSpeed,              routePoints: List.from(_routePoints),              activityType: sessionType.name,            ),          ),        );      }    } else {      await _startTracking();    }  }  // â”€â”€ Save session to Firestore â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€  Future<void> _saveCompletedSession({    required double distance,    required Duration duration,    required double speed,    required int steps,    required double calories,    required TrackActivityType type,  }) async {    final uid = FirebaseAuth.instance.currentUser?.uid;    if (uid == null) return;    try {      await _firestore          .collection('users')          .doc(uid)          .collection('activities')          .add({        'distanceKm': distance,        'durationSeconds': duration.inSeconds,        'avgSpeedKmh': speed,        '\ud83e\uddb6 steps': steps,        'calories': calories,        'type': type.name,        'startTime': FieldValue.serverTimestamp(),      });    } catch (e) {      debugPrint('Failed to save session: $e');    }  }  String _formatDuration(Duration d) {    final h = d.inHours;    final m = d.inMinutes.remainder(60).toString().padLeft(2, '0');    final s = d.inSeconds.remainder(60).toString().padLeft(2, '0');    return h > 0 ? '$h:$m:$s' : '$m:$s';  }  Future<void> _showCompletionDialog() async {    await showDialog<void>(      context: context,      barrierDismissible: false,      builder: (context) {        return Dialog(          backgroundColor: const Color(0xFF131A28),          shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(20)),          child: Padding(            padding: const EdgeInsets.all(22),            child: Column(              mainAxisSize: MainAxisSize.min,              children: [                const Icon(Icons.check_circle, color: Color(0xFFFF6200), size: 52),                const SizedBox(height: 14),                const Text(                  'Activity Complete!',                  style: TextStyle(                    color: Colors.white,                    fontSize: 22,                    fontWeight: FontWeight.w700,                  ),                ),                const SizedBox(height: 8),                Text(                  '${_distanceKm.toStringAsFixed(2)} km  Â·  ${_formatDuration(_elapsed)}  Â·  ${_calories.toStringAsFixed(0)} kcal',                  style: const TextStyle(color: Color(0xFFB2C1D7)),                  textAlign: TextAlign.center,                ),                const SizedBox(height: 18),                SizedBox(                  width: double.infinity,                  child: ElevatedButton(                    onPressed: () => Navigator.pop(context),                    style: ElevatedButton.styleFrom(                      backgroundColor: const Color(0xFFFF6200),                    ),                    child: const Text('Continue'),                  ),                ),              ],            ),          ),        );      },    );  }  // â”€â”€ Like / Comment helpers â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€  void _toggleLike(_SocialPost post) {    setState(() {      if (post.isLiked) {        post.isLiked = false;        post.likes--;      } else {        post.isLiked = true;        post.likes++;      }    });  }  void _addComment(_SocialPost post) {    final controller = TextEditingController();    showModalBottomSheet(      context: context,      isScrollControlled: true,      backgroundColor: const Color(0xFF111F37),      shape: const RoundedRectangleBorder(        borderRadius: BorderRadius.vertical(top: Radius.circular(18)),      ),      builder: (context) {        return Padding(          padding: EdgeInsets.fromLTRB(16, 16, 16, MediaQuery.of(context).viewInsets.bottom + 16),          child: Column(            mainAxisSize: MainAxisSize.min,            children: [              const Text('Add Comment', style: TextStyle(color: Colors.white, fontSize: 18, fontWeight: FontWeight.w700)),              const SizedBox(height: 12),              TextField(                controller: controller,                style: const TextStyle(color: Colors.white),                decoration: InputDecoration(                  hintText: 'Write a comment...',                  hintStyle: const TextStyle(color: Color(0xFF8FA0BA)),                  filled: true,                  fillColor: const Color(0xFF1A2A44),                  border: OutlineInputBorder(borderRadius: BorderRadius.circular(10), borderSide: BorderSide.none),                ),              ),              const SizedBox(height: 10),              SizedBox(                width: double.infinity,                child: ElevatedButton(                  onPressed: () {                    if (controller.text.trim().isNotEmpty) {                      setState(() => post.comments++);                    }                    Navigator.pop(context);                  },                  style: ElevatedButton.styleFrom(backgroundColor: const Color(0xFFFF6200)),                  child: const Text('Post'),                ),              ),            ],          ),        );      },    );  }  // â”€â”€ Build Home tab â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€  Widget _buildHomeTab() {    final name = _profileName.isEmpty ? 'Aditya' : _profileName;    final recentSessions = _activitySessions.take(3).toList();    // Today's totals    final today = DateTime.now();    final todaySessions = _activitySessions.where((s) =>        s.completedAt.year == today.year &&        s.completedAt.month == today.month &&        s.completedAt.day == today.day).toList();    final todaySteps = todaySessions.fold(0, (sum, s) => sum + s.steps);    final todayCalories = todaySessions.fold(0.0, (sum, s) => sum + s.calories);    final todayKm = todaySessions.fold(0.0, (sum, s) => sum + s.distanceKm);    // Goals    const goalSteps = 10000;    const goalCalories = 500.0;    const goalKm = 5.0;    return SafeArea(      child: SingleChildScrollView(        padding: const EdgeInsets.fromLTRB(16, 0, 16, 20),        child: Column(          crossAxisAlignment: CrossAxisAlignment.start,          children: [            // â”€â”€ Header â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€            const SizedBox(height: 16),            Row(              children: [                Expanded(                  child: Column(                    crossAxisAlignment: CrossAxisAlignment.start,                    children: [                      Text(                        'Welcome back \ud83d\udc4b',                        style: TextStyle(                            color: Colors.white.withOpacity(0.7),                            fontSize: 14),                      ),                      Text(                        '$name ðŸ’ª',                                                style: const TextStyle(                            color: Colors.white,                            fontSize: 16,                            fontWeight: FontWeight.w700),                      ),                    ],                  ),                ),                // Search                GestureDetector(                  onTap: () {},                  child: Container(                    width: 40,                    height: 40,                    decoration: BoxDecoration(                      color: const Color(0xFF1A2A44),                      borderRadius: BorderRadius.circular(12),                    ),                    child: const Icon(Icons.search,                        color: Color(0xFF8FA0BA), size: 20),                  ),                ),                const SizedBox(width: 10),                // Chat                GestureDetector(                  onTap: () => Navigator.push(                    context,                    MaterialPageRoute(builder: (_) => const ChatPage()),                  ),                  child: Container(                    width: 40,                    height: 40,                    decoration: BoxDecoration(                      color: const Color(0xFF1A2A44),                      borderRadius: BorderRadius.circular(12),                    ),                    child: const Icon(Icons.chat_bubble_outline,                        color: Color(0xFFFF6200), size: 20),                  ),                ),              ],            ),            const SizedBox(height: 20),            // â”€â”€ Steps Â· Calories Â· Distance side by side â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€            Row(              children: [                Expanded(                  child: _HomeStatTile(                    icon: Icons.directions_walk,                    iconColor: const Color(0xFF4FC3F7),                    label: '\ud83e\uddb6 steps',                    value: '$todaySteps',                    sub: '/ $goalSteps',                  ),                ),                const SizedBox(width: 10),                Expanded(                  child: _HomeStatTile(                    icon: Icons.local_fire_department,                    iconColor: const Color(0xFFFF6200),                    label: 'Calories',                    value: todayCalories.toStringAsFixed(0),                    sub: '/ ${goalCalories.toStringAsFixed(0)} kcal',                  ),                ),                const SizedBox(width: 10),                Expanded(                  child: _HomeStatTile(                    icon: Icons.straighten,                    iconColor: const Color(0xFF66BB6A),                    label: 'Distance',                    value: todayKm.toStringAsFixed(1),                    sub: '/ ${goalKm.toStringAsFixed(0)} km',                  ),                ),              ],            ),            // â”€â”€ Today's Goal â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€            Container(              padding: const EdgeInsets.all(16),              decoration: BoxDecoration(                color: const Color(0xFF1A2A44),                borderRadius: BorderRadius.circular(14),              ),              child: Column(                crossAxisAlignment: CrossAxisAlignment.start,                children: [                  const Text(                    "Today's Goal",                    style: TextStyle(                        color: Colors.white,                        fontSize: 16,                        fontWeight: FontWeight.w700),                  ),                  const SizedBox(height: 14),                  _GoalProgressRow(                    label: '\ud83e\uddb6 steps',                    valueText:                        '$todaySteps / $goalSteps',                    progress: (todaySteps / goalSteps).clamp(0.0, 1.0),                  ),                  _GoalProgressRow(                    label: 'Calories',                    valueText:                        '${todayCalories.toStringAsFixed(0)} / ${goalCalories.toStringAsFixed(0)} kcal',                    progress:                        (todayCalories / goalCalories).clamp(0.0, 1.0),                  ),                  _GoalProgressRow(                    label: 'Distance',                    valueText:                        '${todayKm.toStringAsFixed(1)} / ${goalKm.toStringAsFixed(0)} km',                    progress: (todayKm / goalKm).clamp(0.0, 1.0),                  ),                ],              ),            ),            const SizedBox(height: 18),            // Quick Navigation Cards            Row(              children: [                Expanded(                  child: GestureDetector(                    onTap: () => Navigator.push(context, MaterialPageRoute(builder: (_) => const FoodNutritionPage())),                    child: Container(                      padding: const EdgeInsets.symmetric(vertical: 14, horizontal: 12),                      decoration: BoxDecoration(                        gradient: const LinearGradient(colors: [Color(0xFF1B5E20), Color(0xFF1A2A44)], begin: Alignment.topLeft, end: Alignment.bottomRight),                        borderRadius: BorderRadius.circular(12),                        border: Border.all(color: const Color(0xFF2E7D32)),                      ),                      child: const Row(children: [                        Text('ðŸ¥—', style: TextStyle(fontSize: 22)),                        SizedBox(width: 8),                        Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [                          Text('Food & Nutrition', style: TextStyle(color: Colors.white, fontSize: 13, fontWeight: FontWeight.w700)),                          Text('Track your meals', style: TextStyle(color: Color(0xFF8FA0BA), fontSize: 11)),                        ])),                        Icon(Icons.arrow_forward_ios, color: Color(0xFF8FA0BA), size: 12),                      ]),                    ),                  ),                ),                const SizedBox(width: 10),                Expanded(                  child: GestureDetector(                    onTap: () => setState(() => _currentTab = 1),                    child: Container(                      padding: const EdgeInsets.symmetric(vertical: 14, horizontal: 12),                      decoration: BoxDecoration(                        gradient: const LinearGradient(colors: [Color(0xFF4A148C), Color(0xFF1A2A44)], begin: Alignment.topLeft, end: Alignment.bottomRight),                        borderRadius: BorderRadius.circular(12),                        border: Border.all(color: const Color(0xFF6A1B9A)),                      ),                      child: const Row(children: [                        Text('ðŸƒ', style: TextStyle(fontSize: 22)),                        SizedBox(width: 8),                        Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [                          Text('Start Activity', style: TextStyle(color: Colors.white, fontSize: 13, fontWeight: FontWeight.w700)),                          Text('Track your workout', style: TextStyle(color: Color(0xFF8FA0BA), fontSize: 11)),                        ])),                        Icon(Icons.arrow_forward_ios, color: Color(0xFF8FA0BA), size: 12),                      ]),                    ),                  ),                ),              ],            ),            const SizedBox(height: 18),            // â”€â”€ Activity Summary â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€            _ActivitySummaryRing(              steps: todaySteps,              goalSteps: goalSteps,              calories: todayCalories,              goalCalories: goalCalories,              distanceKm: todayKm,              goalKm: goalKm,            ),            // â”€â”€ Recent Activity â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€            Row(              children: [                const Text(                  '\ud83c\udfc3 Recent Activity',                  style: TextStyle(                      color: Colors.white,                      fontSize: 16,                      fontWeight: FontWeight.w700),                ),                const Spacer(),                TextButton(                  onPressed: () => Navigator.push(                    context,                    MaterialPageRoute(                      builder: (_) => ActivityCalendarPage(                        sessions: _activitySessions,                        userName:                            _profileName.isEmpty ? null : _profileName,                      ),                    ),                  ),                  child: const Text('See All',                      style: TextStyle(color: Color(0xFFFF6200))),                ),              ],            ),            const SizedBox(height: 8),            if (recentSessions.isEmpty)              Container(                padding: const EdgeInsets.all(20),                decoration: BoxDecoration(                  color: const Color(0xFF1A2A44),                  borderRadius: BorderRadius.circular(12),                ),                child: const Center(                  child: Text(                    '\ud83d\ude80 No activities yet. Start tracking!',                    style: TextStyle(color: Color(0xFF8FA0BA)),                  ),                ),              )            else              ...recentSessions.map((s) {                final label = s.activityType == 'running'                    ? '\ud83c\udfc3 Run'                    : s.activityType == '\ud83d\udeb4 Cycling'                        ? '\ud83d\udeb4 Cycling'                        : '\ud83d\udeb6 Walk';                final icon = s.activityType == 'running'                    ? Icons.directions_run                    : s.activityType == '\ud83d\udeb4 Cycling'                        ? Icons.directions_bike                        : Icons.directions_walk;                return Container(                  margin: const EdgeInsets.only(bottom: 10),                  padding: const EdgeInsets.all(12),                  decoration: BoxDecoration(                    color: const Color(0xFF1A2A44),                    borderRadius: BorderRadius.circular(12),                  ),                  child: Row(                    children: [                      CircleAvatar(                        radius: 20,                        backgroundColor: const Color(0xFFFF6200),                        child: Icon(icon, color: Colors.white, size: 20),                      ),                      const SizedBox(width: 12),                      Expanded(                        child: Column(                          crossAxisAlignment: CrossAxisAlignment.start,                          children: [                            Text(label,                                style: const TextStyle(                                    color: Colors.white,                                    fontWeight: FontWeight.w600)),                            Text(                              '${s.distanceKm.toStringAsFixed(2)} km  Â·  ${s.duration}',                              style: const TextStyle(                                  color: Color(0xFF8FA0BA), fontSize: 13),                            ),                          ],                        ),                      ),                      Text(                        '${s.calories.toStringAsFixed(0)} kcal',                        style: const TextStyle(                            color: Color(0xFFFF8B2D),                            fontWeight: FontWeight.w600),                      ),                    ],                  ),                );              }),          ],        ),      ),    );  }  // â”€â”€ Build Track tab â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€  Widget _buildTrackTab() {    return SafeArea(      child: SingleChildScrollView(        padding: const EdgeInsets.fromLTRB(14, 0, 14, 16),        child: Column(          crossAxisAlignment: CrossAxisAlignment.start,          children: [            Container(              width: double.infinity,              padding: const EdgeInsets.symmetric(vertical: 18),              decoration: const BoxDecoration(                gradient: LinearGradient(                  begin: Alignment.topLeft,                  end: Alignment.topRight,                  colors: [Color(0xFFFF5B00), Color(0xFF130E19)],                ),              ),              child: const Text(                'Track Activity',                style: TextStyle(                    color: Colors.white,                    fontSize: 34,                    fontWeight: FontWeight.w700),              ),            ),            const SizedBox(height: 14),            // Activity type selector            Row(              children: [                Expanded(                  child: _ActivityTypeButton(                    icon: Icons.directions_run,                    label: 'Run',                    selected:                        _selectedActivity == TrackActivityType.running,                    onTap: _isTracking                        ? null                        : () => setState(() => _selectedActivity =                            TrackActivityType.running),                  ),                ),                const SizedBox(width: 8),                Expanded(                  child: _ActivityTypeButton(                    icon: Icons.directions_walk,                    label: '\ud83d\udeb6 Walk',                    selected:                        _selectedActivity == TrackActivityType.walking,                    onTap: _isTracking                        ? null                        : () => setState(() => _selectedActivity =                            TrackActivityType.walking),                  ),                ),                const SizedBox(width: 8),                Expanded(                  child: _ActivityTypeButton(                    icon: Icons.directions_bike,                    label: 'Cycle',                    selected:                        _selectedActivity == TrackActivityType.cycling,                    onTap: _isTracking                        ? null                        : () => setState(() => _selectedActivity =                            TrackActivityType.cycling),                  ),                ),              ],            ),            const SizedBox(height: 14),            // GPS status            Container(              padding:                  const EdgeInsets.symmetric(horizontal: 12, vertical: 8),              decoration: BoxDecoration(                color: _gpsAcquired                    ? const Color(0xFF0F2A1A)                    : const Color(0xFF1A2A44),                borderRadius: BorderRadius.circular(10),                border: Border.all(                  color: _gpsAcquired                      ? const Color(0xFF2E7D32)                      : const Color(0xFF314461),                ),              ),              child: Row(                children: [                  Icon(                    _gpsAcquired ? Icons.gps_fixed : Icons.gps_not_fixed,                    color: _gpsAcquired                        ? const Color(0xFF4CAF50)                        : const Color(0xFF8FA0BA),                    size: 18,                  ),                  const SizedBox(width: 8),                  Text(                    _gpsStatusText,                    style: TextStyle(                      color: _gpsAcquired                          ? const Color(0xFF4CAF50)                          : const Color(0xFF8FA0BA),                    ),                  ),                ],              ),            ),            const SizedBox(height: 14),            // Map            ClipRRect(              borderRadius: BorderRadius.circular(14),              child: SizedBox(                height: 220,                child: FlutterMap(                  mapController: _mapController,                  options: MapOptions(                    initialCenter: _currentLatLng ?? const LatLng(0, 0),                    initialZoom: 15,                  ),                  children: [                    TileLayer(                      urlTemplate:                          'https://tile.openstreetmap.org/{z}/{x}/{y}.png',                      userAgentPackageName: 'com.example.build_u',                    ),                    if (_routePoints.length >= 2)                      PolylineLayer(                        polylines: [                          Polyline(                            points: _routePoints,                            color: const Color(0xFFFF5B00),                            strokeWidth: 5,                            strokeCap: StrokeCap.round,                          ),                        ],                      ),                    if (_currentLatLng != null)                      MarkerLayer(                        markers: [                          Marker(                            point: _currentLatLng!,                            width: 20,                            height: 20,                            child: Container(                              decoration: BoxDecoration(                                color: const Color(0xFFFF5B00),                                shape: BoxShape.circle,                                border:                                    Border.all(color: Colors.white, width: 2),                              ),                            ),                          ),                        ],                      ),                  ],                ),              ),            ),            const SizedBox(height: 14),            // Metrics grid            GridView.count(              crossAxisCount: 2,              shrinkWrap: true,              physics: const NeverScrollableScrollPhysics(),              crossAxisSpacing: 10,              mainAxisSpacing: 10,              childAspectRatio: 1.6,              children: [                _TrackMetricCard(                  title: 'Distance',                  value: '${_distanceKm.toStringAsFixed(2)} km',                  icon: Icons.straighten,                ),                _TrackMetricCard(                  title: 'Duration',                  value: _formatDuration(_elapsed),                  icon: Icons.timer_outlined,                ),                _TrackMetricCard(                  title: 'Speed',                  value: '${_currentSpeedKmh.toStringAsFixed(1)} km/h',                  icon: Icons.speed,                ),                _TrackMetricCard(                  title: 'Calories',                  value: '${_calories.toStringAsFixed(0)} kcal',                  icon: Icons.local_fire_department,                ),                _TrackMetricCard(                  title: '\ud83e\uddb6 steps',                  value: '$_steps',                  icon: Icons.directions_walk,                ),              ],            ),            const SizedBox(height: 16),            // Control buttons            if (!_isTracking && !_isPaused)              SizedBox(                width: double.infinity,                child: ElevatedButton.icon(                  onPressed: _toggleTracking,                  icon: const Icon(Icons.play_arrow),                  label: const Text('Start'),                  style: ElevatedButton.styleFrom(                    backgroundColor: const Color(0xFFFF6200),                    foregroundColor: Colors.white,                    minimumSize: const Size.fromHeight(52),                    shape: RoundedRectangleBorder(                        borderRadius: BorderRadius.circular(12)),                  ),                ),              )            else              Row(                children: [                  Expanded(                    child: ElevatedButton.icon(                      onPressed:                          _isPaused ? _resumeTracking : _pauseTracking,                      icon: Icon(                          _isPaused ? Icons.play_arrow : Icons.pause),                      label: Text(_isPaused ? 'Resume' : 'Pause'),                      style: ElevatedButton.styleFrom(                        backgroundColor: const Color(0xFF1A2A44),                        foregroundColor: Colors.white,                        minimumSize: const Size.fromHeight(52),                        shape: RoundedRectangleBorder(                            borderRadius: BorderRadius.circular(12)),                      ),                    ),                  ),                  const SizedBox(width: 10),                  Expanded(                    child: ElevatedButton.icon(                      onPressed: _toggleTracking,                      icon: const Icon(Icons.stop),                      label: const Text('Stop'),                      style: ElevatedButton.styleFrom(                        backgroundColor: const Color(0xFFE53935),                        foregroundColor: Colors.white,                        minimumSize: const Size.fromHeight(52),                        shape: RoundedRectangleBorder(                            borderRadius: BorderRadius.circular(12)),                      ),                    ),                  ),                ],              ),          ],        ),      ),    );  }  // â”€â”€ Build Social tab â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€  Widget _buildSocialTab() {    return SafeArea(      child: LayoutBuilder(        builder: (context, constraints) {          final sidePadding = constraints.maxWidth >= 700 ? 16.0 : 10.0;          return SingleChildScrollView(            padding: EdgeInsets.fromLTRB(sidePadding, 0, sidePadding, 16),            child: Column(              crossAxisAlignment: CrossAxisAlignment.start,              children: [                Container(                  width: double.infinity,                  padding: const EdgeInsets.symmetric(vertical: 18),                  decoration: const BoxDecoration(                    gradient: LinearGradient(                      begin: Alignment.topLeft,                      end: Alignment.topRight,                      colors: [Color(0xFFFF5B00), Color(0xFF130E19)],                    ),                  ),                  child: const Text(                    'Community',                    style: TextStyle(color: Colors.white, fontSize: 34, fontWeight: FontWeight.w700),                  ),                ),                const SizedBox(height: 12),                const Text(                  'Friends',                  style: TextStyle(color: Colors.white, fontSize: 18, fontWeight: FontWeight.w700),                ),                const SizedBox(height: 8),                TextField(                  controller: _friendSearchController,                  style: const TextStyle(color: Colors.white),                  decoration: InputDecoration(                    hintText: 'Search friends...',                    hintStyle: const TextStyle(color: Color(0xFF8FA0BA)),                    prefixIcon: const Icon(Icons.search, color: Color(0xFF8FA0BA)),                    filled: true,                    fillColor: const Color(0xFF1A2A44),                    border: OutlineInputBorder(                      borderRadius: BorderRadius.circular(10),                      borderSide: BorderSide.none,                    ),                  ),                ),                const SizedBox(height: 10),                ..._friendSuggestions.map(                  (friend) => Container(                    margin: const EdgeInsets.only(bottom: 8),                    padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),                    decoration: BoxDecoration(                      color: const Color(0xFF1A2A44),                      borderRadius: BorderRadius.circular(10),                    ),                    child: Row(                      children: [                        const CircleAvatar(radius: 16, child: Icon(Icons.person, size: 16)),                        const SizedBox(width: 8),                        Expanded(                          child: Text(                            friend,                            maxLines: 1,                            overflow: TextOverflow.ellipsis,                            style: const TextStyle(color: Colors.white),                          ),                        ),                        IconButton(                          onPressed: () {},                          icon: const Icon(Icons.person_add_alt_1, color: Color(0xFFFF6200), size: 20),                        ),                      ],                    ),                  ),                ),                const SizedBox(height: 12),                Row(                  children: [                    const Text(                      'Posts',                      style: TextStyle(color: Colors.white, fontSize: 18, fontWeight: FontWeight.w700),                    ),                    const Spacer(),                    IconButton(                      onPressed: () {                        Navigator.push(                          context,                          MaterialPageRoute(builder: (_) => const ChatPage()),                        );                      },                      icon: const Icon(Icons.chat_bubble_outline, color: Color(0xFFFF6200)),                    ),                  ],                ),                ..._posts.map(                  (post) => _SocialPostCard(                    post: post,                    onLike: () => _toggleLike(post),                    onComment: () => _addComment(post),                    onShare: () {                      Navigator.push(                        context,                        MaterialPageRoute(                          builder: (_) => ChatPage(                            prefilledMessage:                                'Check this out from ${post.userName}: ${post.caption}',                          ),                        ),                      );                    },                  ),                ),              ],            ),          );        },      ),    );  }  Future<void> _confirmLeaveEvent(_EventItem event) async {    final shouldLeave = await showDialog<bool>(      context: context,      builder: (context) {        return AlertDialog(          backgroundColor: const Color(0xFF1A2A44),          title: const Text('Leave Event?', style: TextStyle(color: Colors.white)),          content: Text(            'Are you sure you want to leave "${event.title}"?',            style: const TextStyle(color: Color(0xFFB2C1D7)),          ),          actions: [            TextButton(              onPressed: () => Navigator.pop(context, false),              child: const Text('Cancel'),            ),            ElevatedButton(              onPressed: () => Navigator.pop(context, true),              style: ElevatedButton.styleFrom(backgroundColor: const Color(0xFFFF6200)),              child: const Text('Leave'),            ),          ],        );      },    );    if (shouldLeave == true) {      setState(() {        _upcomingEvents.remove(event);        _pastEvents.insert(          0,          _PastEventItem(            title: event.title,            dateLabel: event.dateLabel,            distance: event.locationLabel.split('Â·').length > 1                ? event.locationLabel.split('Â·').last.trim()                : '--',            participants: event.joinedCount,          ),        );      });    }  }  void _toggleJoin(_EventItem event) {    setState(() {      event.joined = !event.joined;      event.joinedCount =          (event.joinedCount + (event.joined ? 1 : -1)).clamp(0, event.capacity);    });  }  Future<void> _pickEventDate() async {    final now = DateTime.now();    final picked = await showDatePicker(      context: context,      initialDate: _eventDate ?? now,      firstDate: now,      lastDate: DateTime(now.year + 5),    );    if (picked != null) setState(() => _eventDate = picked);  }  Future<void> _pickEventTime() async {    final picked = await showTimePicker(      context: context,      initialTime: _eventTime ?? TimeOfDay.now(),    );    if (picked != null) setState(() => _eventTime = picked);  }  void _openCreateEventDialog() {    showModalBottomSheet(      context: context,      isScrollControlled: true,      backgroundColor: const Color(0xFF111F37),      shape: const RoundedRectangleBorder(        borderRadius: BorderRadius.vertical(top: Radius.circular(18)),      ),      builder: (context) {        return StatefulBuilder(          builder: (context, setModalState) {            return Padding(              padding: EdgeInsets.fromLTRB(                16, 16, 16,                MediaQuery.of(context).viewInsets.bottom + 16,              ),              child: SingleChildScrollView(                child: Column(                  crossAxisAlignment: CrossAxisAlignment.stretch,                  mainAxisSize: MainAxisSize.min,                  children: [                    const Text(                      'Create Event',                      style: TextStyle(color: Colors.white, fontSize: 22, fontWeight: FontWeight.w700),                    ),                    const SizedBox(height: 12),                    _modalField(_eventTitleController, 'Event title'),                    const SizedBox(height: 10),                    _modalField(_eventDescriptionController, 'Description'),                    const SizedBox(height: 10),                    _modalField(_eventLocationController, 'Location'),                    const SizedBox(height: 10),                    Row(                      children: [                        Expanded(                          child: OutlinedButton.icon(                            onPressed: () async {                              await _pickEventDate();                              setModalState(() {});                            },                            icon: const Icon(Icons.calendar_month),                            label: Text(                              _eventDate == null                                  ? 'Select Date'                                  : '${_eventDate!.day}/${_eventDate!.month}/${_eventDate!.year}',                            ),                          ),                        ),                        const SizedBox(width: 8),                        Expanded(                          child: OutlinedButton.icon(                            onPressed: () async {                              await _pickEventTime();                              setModalState(() {});                            },                            icon: const Icon(Icons.access_time),                            label: Text(                              _eventTime == null                                  ? 'Select Time'                                  : _eventTime!.format(context),                            ),                          ),                        ),                      ],                    ),                    const SizedBox(height: 12),                    ElevatedButton(                      onPressed: () {                        if (_eventTitleController.text.trim().isEmpty ||                            _eventDate == null ||                            _eventTime == null) return;                        final event = _EventItem(                          title: _eventTitleController.text.trim(),                          type: 'Custom',                          dateLabel:                              '${_eventDate!.day}/${_eventDate!.month}/${_eventDate!.year}',                          timeLabel: _eventTime!.format(context),                          locationLabel: _eventLocationController.text.trim().isEmpty                              ? 'Custom Location'                              : _eventLocationController.text.trim(),                          organizer: 'You',                          joinedCount: 1,                          capacity: 20,                          joined: true,                          accent: const Color(0xFFFF6200),                          participantsPreview: const ['ðŸ™‚'],                          participantOverflow: 0,                          description: _eventDescriptionController.text.trim(),                        );                        setState(() => _upcomingEvents.insert(0, event));                        _eventTitleController.clear();                        _eventDescriptionController.clear();                        _eventLocationController.clear();                        _eventDate = null;                        _eventTime = null;                        Navigator.pop(context);                      },                      style: ElevatedButton.styleFrom(backgroundColor: const Color(0xFFFF6200)),                      child: const Text('Create Event'),                    ),                  ],                ),              ),            );          },        );      },    );  }  Widget _modalField(TextEditingController controller, String hint) {    return TextField(      controller: controller,      style: const TextStyle(color: Colors.white),      decoration: InputDecoration(        hintText: hint,        hintStyle: const TextStyle(color: Color(0xFF8FA0BA)),        filled: true,        fillColor: const Color(0xFF1A2A44),        border: OutlineInputBorder(          borderRadius: BorderRadius.circular(10),          borderSide: BorderSide.none,        ),      ),    );  }  Widget _buildEventsTab() {    return SafeArea(      child: LayoutBuilder(        builder: (context, constraints) {          final sidePadding = constraints.maxWidth >= 700 ? 16.0 : 10.0;          return SingleChildScrollView(            padding: EdgeInsets.fromLTRB(sidePadding, 0, sidePadding, 16),            child: Column(              crossAxisAlignment: CrossAxisAlignment.start,              children: [                Container(                  width: double.infinity,                  padding: const EdgeInsets.symmetric(vertical: 18),                  decoration: const BoxDecoration(                    gradient: LinearGradient(                      begin: Alignment.topLeft,                      end: Alignment.topRight,                      colors: [Color(0xFFFF5B00), Color(0xFF130E19)],                    ),                  ),                  child: Row(                    children: [                      const Expanded(                        child: Column(                          crossAxisAlignment: CrossAxisAlignment.start,                          children: [                            Text(                              'Events',                              style: TextStyle(color: Colors.white, fontSize: 34, fontWeight: FontWeight.w700),                            ),                            SizedBox(height: 4),                            Text(                              'Join group activities and challenges',                              style: TextStyle(color: Color(0xFFFFCCA6), fontSize: 15),                            ),                          ],                        ),                      ),                      ElevatedButton.icon(                        onPressed: _openCreateEventDialog,                        icon: const Icon(Icons.add, size: 16),                        label: const Text('Create'),                        style: ElevatedButton.styleFrom(                          backgroundColor: const Color(0xFF1A1E2A),                          foregroundColor: Colors.white,                          padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 8),                        ),                      ),                    ],                  ),                ),                const SizedBox(height: 12),                Container(                  padding: const EdgeInsets.all(4),                  decoration: BoxDecoration(                    color: const Color(0xFF1A2A44),                    borderRadius: BorderRadius.circular(12),                    border: Border.all(color: const Color(0xFF314461)),                  ),                  child: Row(                    children: [                      Expanded(                        child: _eventTabChip(                          label: 'Upcoming',                          selected: !_showPastEvents,                          onTap: () => setState(() => _showPastEvents = false),                        ),                      ),                      Expanded(                        child: _eventTabChip(                          label: 'Past Events',                          selected: _showPastEvents,                          onTap: () => setState(() => _showPastEvents = true),                        ),                      ),                    ],                  ),                ),                const SizedBox(height: 12),                if (!_showPastEvents)                  ..._upcomingEvents.map(                    (event) => _EventCard(                      event: event,                      onJoinToggle: () => _toggleJoin(event),                      onLeave: event.joined ? () => _confirmLeaveEvent(event) : null,                    ),                  )                else ...[                  ..._pastEvents.map((event) => _PastEventCard(event: event)),                  const SizedBox(height: 12),                  Container(                    width: double.infinity,                    padding: const EdgeInsets.all(20),                    decoration: BoxDecoration(                      color: const Color(0xFF1A2A44),                      borderRadius: BorderRadius.circular(14),                      border: Border.all(color: const Color(0xFF314461)),                    ),                    child: Column(                      children: [                        const Icon(Icons.add, size: 48, color: Color(0xFF71839F)),                        const SizedBox(height: 10),                        const Text(                          'Create Your Own Event',                          style: TextStyle(color: Colors.white, fontSize: 26, fontWeight: FontWeight.w700),                        ),                        const SizedBox(height: 8),                        const Text(                          'Organize a run, walk, or swim session with friends',                          textAlign: TextAlign.center,                          style: TextStyle(color: Color(0xFF9CB0CB), fontSize: 15),                        ),                        const SizedBox(height: 14),                        ElevatedButton.icon(                          onPressed: _openCreateEventDialog,                          icon: const Icon(Icons.add),                          label: const Text('Create Event'),                          style: ElevatedButton.styleFrom(                            backgroundColor: const Color(0xFFFF6200),                            foregroundColor: Colors.white,                            minimumSize: const Size.fromHeight(50),                          ),                        ),                      ],                    ),                  ),                ],              ],            ),          );        },      ),    );  }  Widget _eventTabChip({    required String label,    required bool selected,    required VoidCallback onTap,  }) {    return GestureDetector(      onTap: onTap,      child: Container(        alignment: Alignment.center,        padding: const EdgeInsets.symmetric(vertical: 10),        decoration: BoxDecoration(          color: selected ? const Color(0xFFFF7A00) : Colors.transparent,          borderRadius: BorderRadius.circular(10),        ),        child: Text(          label,          style: TextStyle(            color: selected ? Colors.black : const Color(0xFF8FA0BA),            fontWeight: FontWeight.w700,          ),        ),      ),    );  }  Widget _buildProfileTab() {    final user = FirebaseAuth.instance.currentUser;    final profileName = _profileName.isEmpty ? 'User' : _profileName;    final profileEmail = user?.email ?? 'No email available';    return SafeArea(      child: LayoutBuilder(        builder: (context, constraints) {          final sidePadding = constraints.maxWidth >= 700 ? 16.0 : 10.0;          return SingleChildScrollView(            padding: EdgeInsets.fromLTRB(sidePadding, 0, sidePadding, 16),            child: Column(              crossAxisAlignment: CrossAxisAlignment.stretch,              children: [                Container(                  width: double.infinity,                  padding: const EdgeInsets.symmetric(vertical: 18),                  decoration: const BoxDecoration(                    gradient: LinearGradient(                      begin: Alignment.topLeft,                      end: Alignment.topRight,                      colors: [Color(0xFFFF5B00), Color(0xFF130E19)],                    ),                  ),                  child: Row(                    children: [                      const Expanded(                        child: Text(                          'Profile',                          style: TextStyle(color: Colors.white, fontSize: 34, fontWeight: FontWeight.w700),                        ),                      ),                      IconButton(                        onPressed: () {                          Navigator.push(                            context,                            MaterialPageRoute(builder: (_) => const ProfileViewPage()),                          );                        },                        icon: const Icon(Icons.settings, color: Colors.white),                      ),                      IconButton(                        onPressed: () async {                          try {                            await FirebaseAuth.instance.signOut();                          } catch (_) {                            if (!mounted) return;                            ScaffoldMessenger.of(context).showSnackBar(                              const SnackBar(content: Text('Failed to log out. Please try again.')),                            );                          }                        },                        icon: const Icon(Icons.logout, color: Colors.white),                      ),                    ],                  ),                ),                const SizedBox(height: 12),                Container(                  padding: const EdgeInsets.all(16),                  decoration: BoxDecoration(                    color: const Color(0xFF1A2A44),                    borderRadius: BorderRadius.circular(14),                  ),                  child: Column(                    children: [                      const CircleAvatar(                        radius: 38,                        backgroundColor: Color(0xFFFF6200),                        child: Icon(Icons.person, color: Colors.white, size: 40),                      ),                      const SizedBox(height: 10),                      Text(                        profileName,                        style: const TextStyle(color: Colors.white, fontSize: 32, fontWeight: FontWeight.w700),                      ),                      const SizedBox(height: 4),                      Text(                        profileEmail,                        style: const TextStyle(color: Color(0xFF9CB0CB), fontSize: 16),                      ),                      const SizedBox(height: 8),                      const Text(                        'Fitness enthusiast | Marathon runner | Love to stay active',                        textAlign: TextAlign.center,                        style: TextStyle(color: Colors.white70, fontSize: 15),                      ),                      const SizedBox(height: 12),                      const OutlinedButton(                        onPressed: null,                        child: Text('Edit Profile'),                      ),                    ],                  ),                ),                const SizedBox(height: 12),                Row(                  children: [                    Expanded(child: _ProfileMetricBox(value: _sevenDayKm.toStringAsFixed(1), label: 'Total KM')),                    const SizedBox(width: 10),                    Expanded(child: _ProfileMetricBox(value: '$_sevenDayWorkouts', label: 'Workouts')),                  ],                ),                const SizedBox(height: 10),                Row(                  children: [                    Expanded(child: _ProfileMetricBox(value: _sevenDayCalories.toStringAsFixed(0), label: 'Calories')),                    const SizedBox(width: 10),                    Expanded(child: _ProfileMetricBox(value: '$_dayStreak', label: 'Day Streak')),                  ],                ),                const SizedBox(height: 12),                _DoctorsDashboard(),                const SizedBox(height: 10),                Container(                  padding: const EdgeInsets.all(14),                  decoration: BoxDecoration(                    color: const Color(0xFF1A2A44),                    borderRadius: BorderRadius.circular(14),                  ),                  child: const Column(                    crossAxisAlignment: CrossAxisAlignment.start,                    children: [                      Text(                        'Personal Records',                        style: TextStyle(color: Colors.white, fontSize: 28, fontWeight: FontWeight.w700),                      ),                      SizedBox(height: 12),                      _ProfileRecordRow(label: 'Longest Run', value: '21.1 km'),                      _ProfileRecordRow(label: 'Fastest 5K', value: '24:35 min'),                      _ProfileRecordRow(label: 'Most Steps', value: '18,542'),                    ],                  ),                ),              ],            ),          );        },      ),    );  }  @override  Widget build(BuildContext context) {    return Scaffold(      backgroundColor: const Color(0xFF0B1120),      body: IndexedStack(        index: _currentTab,        children: [          _buildHomeTab(),          _buildTrackTab(),          _buildSocialTab(),          const EventsPage(),          _buildProfileTab(),        ],      ),      bottomNavigationBar: BottomNavigationBar(        currentIndex: _currentTab,        onTap: (i) => setState(() => _currentTab = i),        backgroundColor: const Color(0xFF0F1A2E),        selectedItemColor: const Color(0xFFFF6200),        unselectedItemColor: const Color(0xFF8FA0BA),        type: BottomNavigationBarType.fixed,        items: const [          BottomNavigationBarItem(icon: Icon(Icons.home_outlined), label: 'Home'),          BottomNavigationBarItem(icon: Icon(Icons.directions_run), label: 'Track'),          BottomNavigationBarItem(icon: Icon(Icons.people_outline), label: 'Community'),          BottomNavigationBarItem(icon: Icon(Icons.event_outlined), label: 'Events'),          BottomNavigationBarItem(icon: Icon(Icons.person_outline), label: 'Profile'),        ],      ),    );  }}// â”€â”€ Reusable widgets â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€class _RoundIcon extends StatelessWidget {  const _RoundIcon({required this.icon});  final IconData icon;  @override  Widget build(BuildContext context) {    return CircleAvatar(      radius: 19,      backgroundColor: const Color(0x30232C3E),      child: Icon(icon, color: Colors.white, size: 20),    );  }}class _MiniStatCard extends StatelessWidget {  const _MiniStatCard({required this.icon, required this.title, required this.value});  final IconData icon;  final String title;  final String value;  @override  Widget build(BuildContext context) {    return Container(      padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 10),      decoration: BoxDecoration(        color: const Color(0x222A3D63),        borderRadius: BorderRadius.circular(12),        border: Border.all(color: const Color(0x603D4C67)),      ),      child: Column(        crossAxisAlignment: CrossAxisAlignment.start,        children: [          Icon(icon, color: const Color(0xFFFF8B2D), size: 16),          const SizedBox(height: 4),          Text(title, style: const TextStyle(color: Color(0xFFB9C8DE), fontSize: 13)),          const SizedBox(height: 4),          Text(value, style: const TextStyle(color: Colors.white, fontSize: 26, fontWeight: FontWeight.w600)),        ],      ),    );  }}class _ActivityTypeButton extends StatelessWidget {  const _ActivityTypeButton({    required this.icon,    required this.label,    required this.selected,    this.onTap,  });  final IconData icon;  final String label;  final bool selected;  final VoidCallback? onTap;  @override  Widget build(BuildContext context) {    final disabled = onTap == null;    return InkWell(      onTap: onTap,      borderRadius: BorderRadius.circular(12),      child: Container(        padding: const EdgeInsets.symmetric(vertical: 12),        decoration: BoxDecoration(          color: disabled ? const Color(0xFF111A2A) : const Color(0xFF1A2A44),          borderRadius: BorderRadius.circular(12),          border: Border.all(            color: disabled                ? const Color(0xFF222E44)                : selected                    ? const Color(0xFFFF6200)                    : const Color(0xFF314461),          ),        ),        child: Column(          children: [            Icon(icon,                color: disabled                    ? const Color(0xFF3A4A64)                    : selected                        ? const Color(0xFFFF6200)                        : const Color(0xFF8FA0BA)),            const SizedBox(height: 6),            Text(              label,              style: TextStyle(                color: disabled                    ? const Color(0xFF3A4A64)                    : selected                        ? const Color(0xFFFF6200)                        : const Color(0xFFB2C1D7),                fontWeight: FontWeight.w600,              ),            ),          ],        ),      ),    );  }}class _TrackMetricCard extends StatelessWidget {  const _TrackMetricCard({required this.title, required this.value, required this.icon});  final String title;  final String value;  final IconData icon;  @override  Widget build(BuildContext context) {    return Container(      width: double.infinity,      padding: const EdgeInsets.all(14),      decoration: BoxDecoration(        color: const Color(0xFF1A2A44),        borderRadius: BorderRadius.circular(12),      ),      child: Column(        crossAxisAlignment: CrossAxisAlignment.start,        children: [          Row(            children: [              Icon(icon, color: const Color(0xFFFF6200), size: 16),              const SizedBox(width: 6),              Text(title, style: const TextStyle(color: Color(0xFFAAB3C3), fontSize: 16)),            ],          ),          const SizedBox(height: 14),          Text(value, style: const TextStyle(color: Colors.white, fontSize: 34, fontWeight: FontWeight.w600)),        ],      ),    );  }}class _SocialPost {  _SocialPost({    required this.userName,    required this.timeAgo,    required this.caption,    required this.likes,    required this.comments,    required this.distance,    required this.duration,    required this.calories,    this.imageUrl,    this.imagePath,    this.location,    this.isLiked = false,  });  final String userName;  final String timeAgo;  final String caption;  final String? imageUrl;  final String? imagePath;  final String? location;  int likes;  int comments;  bool isLiked;  final String distance;  final String duration;  final String calories;}class _SocialPostCard extends StatelessWidget {  const _SocialPostCard({    required this.post,    required this.onLike,    required this.onComment,    required this.onShare,  });  final _SocialPost post;  final VoidCallback onLike;  final VoidCallback onComment;  final VoidCallback onShare;  @override  Widget build(BuildContext context) {    return Container(      margin: const EdgeInsets.only(bottom: 12),      decoration: BoxDecoration(        color: const Color(0xFF1A2A44),        borderRadius: BorderRadius.circular(14),      ),      child: Column(        crossAxisAlignment: CrossAxisAlignment.stretch,        children: [          Padding(            padding: const EdgeInsets.all(12),            child: Row(              children: [                const CircleAvatar(radius: 16, child: Icon(Icons.person, size: 16)),                const SizedBox(width: 10),                Expanded(                  child: Column(                    crossAxisAlignment: CrossAxisAlignment.start,                    children: [                      Text(post.userName,                          style: const TextStyle(color: Colors.white, fontWeight: FontWeight.w600)),                      Text(post.timeAgo,                          style: const TextStyle(color: Color(0xFF8FA0BA), fontSize: 12)),                    ],                  ),                ),                const Icon(Icons.more_horiz, color: Color(0xFF8FA0BA)),              ],            ),          ),          Padding(            padding: const EdgeInsets.symmetric(horizontal: 12),            child: Text(post.caption, style: const TextStyle(color: Colors.white, fontSize: 16)),          ),          if ((post.location ?? '').isNotEmpty)            Padding(              padding: const EdgeInsets.fromLTRB(12, 6, 12, 0),              child: Text('ðŸ“ ${post.location}',                  style: const TextStyle(color: Color(0xFF8FA0BA), fontSize: 12)),            ),          const SizedBox(height: 10),          Container(            height: 180,            color: const Color(0xFF2C3E5E),            child: post.imagePath != null                ? Center(                    child: Padding(                      padding: const EdgeInsets.all(10),                      child: Text(post.imagePath!,                          style: const TextStyle(color: Color(0xFFB2C1D7), fontSize: 12)),                    ),                  )                : post.imageUrl != null                    ? Image.network(post.imageUrl!, fit: BoxFit.cover)                    : const SizedBox.shrink(),          ),          Container(            padding: const EdgeInsets.symmetric(vertical: 10, horizontal: 12),            color: const Color(0xFF142841),            child: Row(              children: [                Expanded(child: _PostStat(label: 'Distance', value: post.distance)),                Expanded(child: _PostStat(label: 'Time', value: post.duration)),                Expanded(child: _PostStat(label: 'Calories', value: post.calories)),              ],            ),          ),          Padding(            padding: const EdgeInsets.fromLTRB(12, 10, 12, 4),            child: Row(              children: [                Text('${post.likes} likes', style: const TextStyle(color: Color(0xFF8FA0BA))),                const Spacer(),                Text('${post.comments} comments', style: const TextStyle(color: Color(0xFF8FA0BA))),              ],            ),          ),          Row(            children: [              Expanded(                child: TextButton.icon(                  onPressed: onLike,                  icon: Icon(                    post.isLiked ? Icons.favorite : Icons.favorite_border,                    color: post.isLiked ? const Color(0xFFFF6200) : const Color(0xFFB2C1D7),                  ),                  label: Text('Like',                      style: TextStyle(                        color: post.isLiked ? const Color(0xFFFF6200) : const Color(0xFFB2C1D7),                      )),                ),              ),              Expanded(                child: TextButton.icon(                  onPressed: onComment,                  icon: const Icon(Icons.mode_comment_outlined, color: Color(0xFFB2C1D7)),                  label: const Text('Comment', style: TextStyle(color: Color(0xFFB2C1D7))),                ),              ),              Expanded(                child: TextButton.icon(                  onPressed: onShare,                  icon: const Icon(Icons.share_outlined, color: Color(0xFFB2C1D7)),                  label: const Text('Share', style: TextStyle(color: Color(0xFFB2C1D7))),                ),              ),            ],          ),        ],      ),    );  }}class _PostStat extends StatelessWidget {  const _PostStat({required this.label, required this.value});  final String label;  final String value;  @override  Widget build(BuildContext context) {    return Column(      crossAxisAlignment: CrossAxisAlignment.start,      children: [        Text(label, style: const TextStyle(color: Color(0xFF8FA0BA), fontSize: 12)),        Text(value, style: const TextStyle(color: Colors.white, fontWeight: FontWeight.w600)),      ],    );  }}class _EventItem {  _EventItem({    required this.title,    required this.type,    required this.dateLabel,    required this.timeLabel,    required this.locationLabel,    required this.organizer,    required this.joinedCount,    required this.capacity,    required this.joined,    required this.accent,    required this.participantsPreview,    required this.participantOverflow,    this.description,  });  final String title;  final String type;  final String dateLabel;  final String timeLabel;  final String locationLabel;  final String organizer;  int joinedCount;  final int capacity;  bool joined;  final Color accent;  final List<String> participantsPreview;  final int participantOverflow;  final String? description;}class _PastEventItem {  const _PastEventItem({    required this.title,    required this.dateLabel,    required this.distance,    required this.participants,  });  final String title;  final String dateLabel;  final String distance;  final int participants;}class _EventCard extends StatelessWidget {  const _EventCard({    required this.event,    required this.onJoinToggle,    this.onLeave,  });  final _EventItem event;  final VoidCallback onJoinToggle;  final VoidCallback? onLeave;  @override  Widget build(BuildContext context) {    return Container(      margin: const EdgeInsets.only(bottom: 12),      decoration: BoxDecoration(        color: const Color(0xFF1A2A44),        borderRadius: BorderRadius.circular(14),      ),      child: Column(        crossAxisAlignment: CrossAxisAlignment.stretch,        children: [          Container(            padding: const EdgeInsets.all(12),            decoration: BoxDecoration(              color: event.accent,              borderRadius: const BorderRadius.vertical(top: Radius.circular(14)),            ),            child: Row(              children: [                const CircleAvatar(                  radius: 20,                  backgroundColor: Color(0x33FFFFFF),                  child: Icon(Icons.north_east, color: Colors.white),                ),                const SizedBox(width: 10),                Expanded(                  child: Column(                    crossAxisAlignment: CrossAxisAlignment.start,                    children: [                      Text(event.title,                          style: const TextStyle(                              color: Colors.white, fontSize: 22, fontWeight: FontWeight.w700)),                      Container(                        margin: const EdgeInsets.only(top: 4),                        padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 2),                        decoration: BoxDecoration(                          color: const Color(0x44FFFFFF),                          borderRadius: BorderRadius.circular(6),                        ),                        child: Text(event.type,                            style: const TextStyle(color: Colors.white, fontSize: 12)),                      ),                    ],                  ),                ),              ],            ),          ),          Padding(            padding: const EdgeInsets.all(12),            child: Column(              crossAxisAlignment: CrossAxisAlignment.start,              children: [                Row(                  children: [                    const Icon(Icons.calendar_today, color: Color(0xFF8FA0BA), size: 16),                    const SizedBox(width: 6),                    Text(event.dateLabel, style: const TextStyle(color: Colors.white70)),                    const SizedBox(width: 14),                    const Icon(Icons.access_time, color: Color(0xFF8FA0BA), size: 16),                    const SizedBox(width: 6),                    Text(event.timeLabel, style: const TextStyle(color: Colors.white70)),                  ],                ),                const SizedBox(height: 8),                Row(                  children: [                    const Icon(Icons.location_on_outlined, color: Color(0xFF8FA0BA), size: 16),                    const SizedBox(width: 6),                    Text(event.locationLabel, style: const TextStyle(color: Colors.white70)),                  ],                ),                const Divider(height: 20, color: Color(0x333A4C68)),                Row(                  children: [                    const CircleAvatar(radius: 14, child: Icon(Icons.person, size: 14)),                    const SizedBox(width: 8),                    Column(                      crossAxisAlignment: CrossAxisAlignment.start,                      children: [                        const Text('Organized by', style: TextStyle(color: Color(0xFF8FA0BA))),                        Text(event.organizer, style: const TextStyle(color: Colors.white)),                      ],                    ),                  ],                ),                if ((event.description ?? '').isNotEmpty) ...[                  const SizedBox(height: 8),                  Text(event.description!, style: const TextStyle(color: Color(0xFFB2C1D7))),                ],                const SizedBox(height: 10),                Row(                  children: [                    Text('${event.joinedCount} / ${event.capacity} joined',                        style: const TextStyle(color: Color(0xFF9CB0CB))),                    const Spacer(),                    ...event.participantsPreview.map(                      (icon) => Padding(                        padding: const EdgeInsets.only(left: 3),                        child: Text(icon, style: const TextStyle(fontSize: 18)),                      ),                    ),                    if (event.participantOverflow > 0)                      Padding(                        padding: const EdgeInsets.only(left: 4),                        child: Container(                          padding: const EdgeInsets.symmetric(horizontal: 6, vertical: 2),                          decoration: BoxDecoration(                            color: const Color(0xFF33445F),                            borderRadius: BorderRadius.circular(8),                          ),                          child: Text('+${event.participantOverflow}',                              style: const TextStyle(color: Color(0xFFB2C1D7), fontSize: 12)),                        ),                      ),                  ],                ),                const SizedBox(height: 10),                SizedBox(                  width: double.infinity,                  child: ElevatedButton(                    onPressed: event.joined ? onLeave : onJoinToggle,                    style: ElevatedButton.styleFrom(                      backgroundColor:                          event.joined ? const Color(0xFF465A78) : const Color(0xFFFF6200),                      foregroundColor: Colors.white,                    ),                    child: Text(event.joined ? 'Leave Event' : 'Join Event'),                  ),                ),              ],            ),          ),        ],      ),    );  }}class _PastEventCard extends StatelessWidget {  const _PastEventCard({required this.event});  final _PastEventItem event;  @override  Widget build(BuildContext context) {    return Container(      margin: const EdgeInsets.only(bottom: 10),      padding: const EdgeInsets.all(12),      decoration: BoxDecoration(        color: const Color(0xFF1A2A44),        borderRadius: BorderRadius.circular(12),      ),      child: Row(        children: [          const CircleAvatar(            radius: 18,            backgroundColor: Color(0xFFFF7A00),            child: Icon(Icons.north_east, color: Colors.white),          ),          const SizedBox(width: 10),          Expanded(            child: Column(              crossAxisAlignment: CrossAxisAlignment.start,              children: [                Text(event.title,                    style: const TextStyle(                        color: Colors.white, fontSize: 18, fontWeight: FontWeight.w700)),                Text(event.dateLabel, style: const TextStyle(color: Color(0xFF8FA0BA))),              ],            ),          ),          Column(            crossAxisAlignment: CrossAxisAlignment.end,            children: [              Text(event.distance, style: const TextStyle(color: Colors.white)),              Text('${event.participants} participants',                  style: const TextStyle(color: Color(0xFF8FA0BA))),            ],          ),        ],      ),    );  }}class _ProfileMetricBox extends StatelessWidget {  const _ProfileMetricBox({required this.value, required this.label});  final String value;  final String label;  @override  Widget build(BuildContext context) {    return Container(      padding: const EdgeInsets.symmetric(vertical: 16),      decoration: BoxDecoration(        color: const Color(0xFF1A2A44),        borderRadius: BorderRadius.circular(14),      ),      child: Column(        children: [          Text(value,              style: const TextStyle(                  color: Color(0xFFFF7A00), fontSize: 34, fontWeight: FontWeight.w700)),          const SizedBox(height: 8),          Text(label, style: const TextStyle(color: Color(0xFF9CB0CB), fontSize: 16)),        ],      ),    );  }}class _ProfileRecordRow extends StatelessWidget {  const _ProfileRecordRow({required this.label, required this.value});  final String label;  final String value;  @override  Widget build(BuildContext context) {    return Padding(      padding: const EdgeInsets.only(bottom: 8),      child: Row(        children: [          Text(label, style: const TextStyle(color: Color(0xFF9CB0CB), fontSize: 16)),          const Spacer(),          Text(value,              style: const TextStyle(                  color: Colors.white, fontSize: 16, fontWeight: FontWeight.w600)),        ],      ),    );  }}class _GoalProgressRow extends StatelessWidget {  const _GoalProgressRow({    required this.label,    required this.valueText,    required this.progress,  });  final String label;  final String valueText;  final double progress;  @override  Widget build(BuildContext context) {    return Padding(      padding: const EdgeInsets.only(bottom: 10),      child: Column(        children: [          Row(            children: [              Text(label, style: const TextStyle(color: Color(0xFFAAB3C3))),              const Spacer(),              Text(valueText, style: const TextStyle(color: Color(0xFFAAB3C3))),            ],          ),          const SizedBox(height: 6),          LinearProgressIndicator(            value: progress,            minHeight: 6,            backgroundColor: const Color(0xFF3E4F69),            valueColor: const AlwaysStoppedAnimation(Color(0xFF214A89)),            borderRadius: BorderRadius.circular(50),          ),        ],      ),    );  }}class _ActivityRings extends StatelessWidget {  const _ActivityRings();  @override  Widget build(BuildContext context) {    return SizedBox(      height: 170,      width: 170,      child: Stack(        alignment: Alignment.center,        children: [          _Ring(progress: 0.82, size: 170, stroke: 8, color: const Color(0xFFFF7D1A)),          _Ring(progress: 0.70, size: 140, stroke: 8, color: const Color(0xFFFF6200)),          _Ring(progress: 0.62, size: 110, stroke: 8, color: const Color(0xFFF5C433)),        ],      ),    );  }}class _Ring extends StatelessWidget {  const _Ring({    required this.progress,    required this.size,    required this.stroke,    required this.color,  });  final double progress;  final double size;  final double stroke;  final Color color;  @override  Widget build(BuildContext context) {    return SizedBox(      height: size,      width: size,      child: CircularProgressIndicator(        value: progress,        strokeWidth: stroke,        backgroundColor: const Color(0xFF41526D),        valueColor: AlwaysStoppedAnimation<Color>(color),      ),    );  }}class _LegendDot extends StatelessWidget {  const _LegendDot({required this.color, required this.label});  final Color color;  final String label;  @override  Widget build(BuildContext context) {    return Row(      children: [        CircleAvatar(radius: 4, backgroundColor: color),        const SizedBox(width: 5),        Text(label, style: const TextStyle(color: Color(0xFFAAB3C3), fontSize: 12)),      ],    );  }}class _ActivitySummaryRing extends StatefulWidget {  const _ActivitySummaryRing({    required this.steps,    required this.goalSteps,    required this.calories,    required this.goalCalories,    required this.distanceKm,    required this.goalKm,  });  final int steps;  final int goalSteps;  final double calories;  final double goalCalories;  final double distanceKm;  final double goalKm;  @override  State<_ActivitySummaryRing> createState() => _ActivitySummaryRingState();}class _ActivitySummaryRingState extends State<_ActivitySummaryRing>    with SingleTickerProviderStateMixin {  late AnimationController _controller;  late Animation<double> _stepsAnim;  late Animation<double> _calAnim;  late Animation<double> _distAnim;  @override  void initState() {    super.initState();    _controller = AnimationController(        vsync: this, duration: const Duration(milliseconds: 900));    _stepsAnim = Tween<double>(begin: 0,        end: (widget.steps / widget.goalSteps).clamp(0.0, 1.0))        .animate(CurvedAnimation(parent: _controller, curve: Curves.easeOut));    _calAnim = Tween<double>(begin: 0,        end: (widget.calories / widget.goalCalories).clamp(0.0, 1.0))        .animate(CurvedAnimation(parent: _controller, curve: Curves.easeOut));    _distAnim = Tween<double>(begin: 0,        end: (widget.distanceKm / widget.goalKm).clamp(0.0, 1.0))        .animate(CurvedAnimation(parent: _controller, curve: Curves.easeOut));    _controller.forward();  }  @override  void didUpdateWidget(_ActivitySummaryRing old) {    super.didUpdateWidget(old);    // Re-animate whenever steps (or other values) change    _stepsAnim = Tween<double>(        begin: _stepsAnim.value,        end: (widget.steps / widget.goalSteps).clamp(0.0, 1.0))        .animate(CurvedAnimation(parent: _controller, curve: Curves.easeOut));    _calAnim = Tween<double>(        begin: _calAnim.value,        end: (widget.calories / widget.goalCalories).clamp(0.0, 1.0))        .animate(CurvedAnimation(parent: _controller, curve: Curves.easeOut));    _distAnim = Tween<double>(        begin: _distAnim.value,        end: (widget.distanceKm / widget.goalKm).clamp(0.0, 1.0))        .animate(CurvedAnimation(parent: _controller, curve: Curves.easeOut));    _controller      ..reset()      ..forward();  }  @override  void dispose() {    _controller.dispose();    super.dispose();  }  @override  Widget build(BuildContext context) {    return Container(      padding: const EdgeInsets.all(16),      decoration: BoxDecoration(        color: const Color(0xFF1A2A44),        borderRadius: BorderRadius.circular(14),      ),      child: Column(        crossAxisAlignment: CrossAxisAlignment.start,        children: [          const Text(            '\ud83d\udcca Activity Summary',            style: TextStyle(                color: Colors.white,                fontSize: 16,                fontWeight: FontWeight.w700),          ),          const SizedBox(height: 16),          AnimatedBuilder(            animation: _controller,            builder: (context, _) {              return Row(                children: [                  // Rings                  SizedBox(                    width: 140,                    height: 140,                    child: CustomPaint(                      painter: _ThreeRingPainter(                        stepsProgress: _stepsAnim.value,                        calProgress: _calAnim.value,                        distProgress: _distAnim.value,                      ),                      child: Center(                        child: Column(                          mainAxisSize: MainAxisSize.min,                          children: [                            Text(                              '${widget.steps}',                              style: const TextStyle(                                  color: Colors.white,                                  fontSize: 22,                                  fontWeight: FontWeight.w700),                            ),                            const Text(                              '\ud83e\uddb6 steps',                              style: TextStyle(                                  color: Color(0xFF8FA0BA),                                  fontSize: 12),                            ),                          ],                        ),                      ),                    ),                  ),                  const SizedBox(width: 20),                  // Legend                  Expanded(                    child: Column(                      crossAxisAlignment: CrossAxisAlignment.start,                      children: [                        _RingLegendRow(                          color: const Color(0xFFFF7D1A),                          label: '\ud83e\uddb6 steps',                          value: '${widget.steps} / ${widget.goalSteps}',                          progress: _stepsAnim.value,                        ),                        const SizedBox(height: 12),                        _RingLegendRow(                          color: const Color(0xFFFF6200),                          label: '\ud83d\udd25 Calories',                          value:                              '${widget.calories.toStringAsFixed(0)} / ${widget.goalCalories.toStringAsFixed(0)}',                          progress: _calAnim.value,                        ),                        const SizedBox(height: 12),                        _RingLegendRow(                          color: const Color(0xFFF5C433),                          label: '\ud83d\udccd Distance',                          value:                              '${widget.distanceKm.toStringAsFixed(1)} / ${widget.goalKm.toStringAsFixed(0)} km',                          progress: _distAnim.value,                        ),                      ],                    ),                  ),                ],              );            },          ),        ],      ),    );  }}class _ThreeRingPainter extends CustomPainter {  const _ThreeRingPainter({    required this.stepsProgress,    required this.calProgress,    required this.distProgress,  });  final double stepsProgress;  final double calProgress;  final double distProgress;  @override  void paint(Canvas canvas, Size size) {    final cx = size.width / 2;    final cy = size.height / 2;    const stroke = 10.0;    const gap = 14.0;    final radii = [cx - stroke / 2, cx - stroke / 2 - gap, cx - stroke / 2 - gap * 2];    final colors = [const Color(0xFFFF7D1A), const Color(0xFFFF6200), const Color(0xFFF5C433)];    final progresses = [stepsProgress, calProgress, distProgress];    const startAngle = -math.pi / 2;    for (int i = 0; i < 3; i++) {      final rect = Rect.fromCircle(center: Offset(cx, cy), radius: radii[i]);      // Track      canvas.drawArc(        rect,        0,        math.pi * 2,        false,        Paint()          ..color = const Color(0xFF2A3A54)          ..style = PaintingStyle.stroke          ..strokeWidth = stroke          ..strokeCap = StrokeCap.round,      );      // Progress      if (progresses[i] > 0) {        canvas.drawArc(          rect,          startAngle,          math.pi * 2 * progresses[i],          false,          Paint()            ..color = colors[i]            ..style = PaintingStyle.stroke            ..strokeWidth = stroke            ..strokeCap = StrokeCap.round,        );      }    }  }  @override  bool shouldRepaint(_ThreeRingPainter old) =>      old.stepsProgress != stepsProgress ||      old.calProgress != calProgress ||      old.distProgress != distProgress;}class _RingLegendRow extends StatelessWidget {  const _RingLegendRow({    required this.color,    required this.label,    required this.value,    required this.progress,  });  final Color color;  final String label;  final String value;  final double progress;  @override  Widget build(BuildContext context) {    return Column(      crossAxisAlignment: CrossAxisAlignment.start,      children: [        Row(          children: [            CircleAvatar(radius: 5, backgroundColor: color),            const SizedBox(width: 6),            Text(label,                style: const TextStyle(                    color: Color(0xFFAAB3C3), fontSize: 12)),            const Spacer(),            Text(              '${(progress * 100).toStringAsFixed(0)}%',              style: TextStyle(                  color: color,                  fontSize: 12,                  fontWeight: FontWeight.w700),            ),          ],        ),        const SizedBox(height: 4),        Text(value,            style: const TextStyle(                color: Colors.white,                fontSize: 13,                fontWeight: FontWeight.w600)),      ],    );  }}class _HomeStatTile extends StatelessWidget {  const _HomeStatTile({    required this.icon,    required this.iconColor,    required this.label,    required this.value,    required this.sub,  });  final IconData icon;  final Color iconColor;  final String label;  final String value;  final String sub;  @override  Widget build(BuildContext context) {    return Container(      padding: const EdgeInsets.symmetric(vertical: 10, horizontal: 8),      decoration: BoxDecoration(        color: const Color(0xFF1A2A44),        borderRadius: BorderRadius.circular(12),        border: Border.all(color: const Color(0xFF2A3A54)),      ),      child: Column(        mainAxisSize: MainAxisSize.min,        children: [          Container(            padding: const EdgeInsets.all(6),            decoration: BoxDecoration(              color: iconColor.withOpacity(0.15),              shape: BoxShape.circle,            ),            child: Icon(icon, color: iconColor, size: 16),          ),          const SizedBox(height: 6),          Text(            value,            style: const TextStyle(                color: Colors.white,                fontSize: 15,                fontWeight: FontWeight.w700),          ),          Text(            sub,            style: const TextStyle(                color: Color(0xFF8FA0BA), fontSize: 9),            textAlign: TextAlign.center,            maxLines: 1,            overflow: TextOverflow.ellipsis,          ),          const SizedBox(height: 2),          Text(            label,            style: const TextStyle(                color: Color(0xFFAAB3C3), fontSize: 10),          ),        ],      ),    );  }}class _SummaryMetric extends StatelessWidget {  const _SummaryMetric({required this.label, required this.value});  final String label;  final String value;  @override  Widget build(BuildContext context) {    return Column(      children: [        Text(          value,          style: const TextStyle(              color: Colors.white,              fontSize: 18,              fontWeight: FontWeight.w700),        ),        const SizedBox(height: 4),        Text(          label,          style: const TextStyle(              color: Color(0xFFFFCCA6), fontSize: 11),        ),      ],    );  }}// â”€â”€ Doctor Dashboard â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€class _DoctorsDashboard extends StatefulWidget {  @override  State<_DoctorsDashboard> createState() => _DoctorsDashboardState();}class _DoctorsDashboardState extends State<_DoctorsDashboard> {  String? _assignedDoctorId;  @override  void initState() {    super.initState();    _loadAssigned();  }  Future<void> _loadAssigned() async {    final uid = FirebaseAuth.instance.currentUser?.uid;    if (uid == null) return;    final doc =        await FirebaseFirestore.instance.collection('users').doc(uid).get();    if (!mounted) return;    setState(() {      _assignedDoctorId = doc.data()?['assignedDoctorId'] as String?;    });  }  Future<void> _assignDoctor(String doctorId) async {    final uid = FirebaseAuth.instance.currentUser?.uid;    if (uid == null) return;    try {      await FirebaseFirestore.instance          .collection('users')          .doc(uid)          .set({'assignedDoctorId': doctorId}, SetOptions(merge: true));      if (!mounted) return;      setState(() => _assignedDoctorId = doctorId);      ScaffoldMessenger.of(context).showSnackBar(const SnackBar(        content: Text('Doctor connected!', style: TextStyle(color: Colors.white)),        backgroundColor: Color(0xFF1B5E20),      ));    } catch (e) {      if (!mounted) return;      ScaffoldMessenger.of(context).showSnackBar(SnackBar(        content: Text('Failed: $e', style: const TextStyle(color: Colors.white)),        backgroundColor: Colors.redAccent,      ));    }  }  Future<void> _removeDoctor() async {    final uid = FirebaseAuth.instance.currentUser?.uid;    if (uid == null) return;    await FirebaseFirestore.instance        .collection('users')        .doc(uid)        .set({'assignedDoctorId': FieldValue.delete()}, SetOptions(merge: true));    if (!mounted) return;    setState(() => _assignedDoctorId = null);  }  @override  Widget build(BuildContext context) {    return Container(      padding: const EdgeInsets.all(14),      decoration: BoxDecoration(        color: const Color(0xFF1A2A44),        borderRadius: BorderRadius.circular(14),      ),      child: Column(        crossAxisAlignment: CrossAxisAlignment.start,        children: [          const Text('My Doctors',              style: TextStyle(color: Colors.white, fontSize: 20, fontWeight: FontWeight.w700)),          const SizedBox(height: 4),          const Text('Choose a doctor to share your activity',              style: TextStyle(color: Color(0xFF8FA0BA), fontSize: 13)),          const SizedBox(height: 12),          StreamBuilder<QuerySnapshot<Map<String, dynamic>>>(            stream: FirebaseFirestore.instance.collection('doctors').snapshots(),            builder: (context, snapshot) {              if (snapshot.connectionState == ConnectionState.waiting) {                return const Center(child: CircularProgressIndicator());              }              if (snapshot.hasError) {                return Text('Error: ${snapshot.error}',                    style: const TextStyle(color: Colors.redAccent));              }              final docs = snapshot.data?.docs ?? [];              if (docs.isEmpty) {                return const Text('No doctors available yet.',                    style: TextStyle(color: Color(0xFFAAB3C3)));              }              return Column(                children: docs.map((doc) {                  final data = doc.data();                  final name = (data['name'] as String? ?? 'Doctor').trim();                  final degree = data['degree'] as String? ?? '';                  final qualification = data['qualification'] as String? ?? '';                  final clinicLocation = data['clinicLocation'] as String? ?? '';                  final isAssigned = _assignedDoctorId == doc.id;                  return Container(                    margin: const EdgeInsets.only(bottom: 10),                    padding: const EdgeInsets.all(12),                    decoration: BoxDecoration(                      color: isAssigned                          ? const Color(0xFF0F2A1A)                          : const Color(0xFF0F1E35),                      borderRadius: BorderRadius.circular(12),                      border: Border.all(                        color: isAssigned                            ? const Color(0xFF2E7D32)                            : const Color(0xFF2A3A54),                      ),                    ),                    child: Row(                      children: [                        CircleAvatar(                          radius: 22,                          backgroundColor:                              isAssigned ? const Color(0xFF2E7D32) : const Color(0xFFFF6200),                          child: const Icon(Icons.medical_services,                              color: Colors.white, size: 22),                        ),                        const SizedBox(width: 12),                        Expanded(                          child: Column(                            crossAxisAlignment: CrossAxisAlignment.start,                            children: [                              Text('Dr. $name',                                  style: const TextStyle(                                      color: Colors.white,                                      fontSize: 16,                                      fontWeight: FontWeight.w600)),                              if (degree.isNotEmpty || qualification.isNotEmpty)                                Text(                                  [degree, qualification]                                      .where((s) => s.isNotEmpty)                                      .join(' Â· '),                                  style: const TextStyle(                                      color: Color(0xFF8FA0BA), fontSize: 12),                                ),                              if (clinicLocation.isNotEmpty)                                Text('ðŸ“ $clinicLocation',                                    style: const TextStyle(                                        color: Color(0xFF8FA0BA), fontSize: 12)),                            ],                          ),                        ),                        const SizedBox(width: 8),                        isAssigned                            ? TextButton(                                onPressed: _removeDoctor,                                child: const Text('Remove',                                    style: TextStyle(color: Colors.redAccent)))                            : ElevatedButton(                                onPressed: () => _assignDoctor(doc.id),                                style: ElevatedButton.styleFrom(                                  backgroundColor: const Color(0xFFFF6200),                                  foregroundColor: Colors.white,                                  padding: const EdgeInsets.symmetric(                                      horizontal: 12, vertical: 8),                                  textStyle: const TextStyle(fontSize: 13),                                ),                                child: const Text('Connect'),                              ),                      ],                    ),                  );                }).toList(),              );            },          ),        ],      ),    );  }}
